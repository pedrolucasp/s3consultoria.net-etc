[Unit]
Description=VaultWarden Container Service
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker start -a vaultwarden
ExecStop=/usr/bin/docker stop -t 5 vaultwarden

[Install]
WantedBy=local.target
